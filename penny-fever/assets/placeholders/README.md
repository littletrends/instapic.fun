# Placeholders

SVG stand-ins keyed to `suggested_asset_id` in `../MEDIA_MANIFEST.md` (and root `MEDIA_MANIFEST.md`).

**Codex:** replace the file at the same path (or update `assets/asset-map.json`) with prepared media. Do not rename IDs without updating `asset-map.json` + HTML.

Aura builds against these paths only.
